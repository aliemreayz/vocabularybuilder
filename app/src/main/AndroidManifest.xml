<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vocabulary">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vocabulary">
        <activity android:name=".NatificationDelet"></activity>
        <activity android:name=".NotificationDelete" />
        <activity android:name=".ListWordActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".DeleteActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".WordActivity" />
        <activity android:name=".WordsActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".CategoriesActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".Notification_reciever" />
        <receiver android:name=".Notification_reciever2" />
    </application>

</manifest>